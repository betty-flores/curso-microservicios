server:
  port: 8081

srping:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/betty-flores/ms-course-youtube.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

